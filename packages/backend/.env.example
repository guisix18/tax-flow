# Fornecida automaticamente pelo add-on PostgreSQL do Railway
DATABASE_URL="postgresql://user:password@host:5432/railway"

# Segredo para assinar os JWT — gere com: openssl rand -hex 32
JWT_SECRET="troque-por-um-segredo-longo-e-aleatorio"

# Mailjet — gere em app.mailjet.com > Account > API Keys
MAILJET_API_KEY="xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"
MAILJET_SECRET_KEY="xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"
# Remetente — precisa ser validado em app.mailjet.com > Senders & Domains > Add Sender
MAILJET_FROM_EMAIL="seuemail@gmail.com"
MAILJET_FROM_NAME="Tax Flow"

# Opcional: quantos dias antes do vencimento enviar lembrete (padrão 3)
REMINDER_DAYS_AHEAD=3

# Railway define PORT automaticamente — não precisa configurar
# PORT=3333

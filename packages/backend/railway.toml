[build]
builder = "nixpacks"
buildCommand = "npm install && npm run build"

[deploy]
startCommand = "cd packages/backend && npx prisma migrate deploy && npx tsx src/server.ts"
restartPolicyType = "on_failure"
